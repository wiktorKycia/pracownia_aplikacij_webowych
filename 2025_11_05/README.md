# Express + Mysql + Docker

## Instruction
Run this command in your shell to see my work
```bash
cd 2025_11_05
docker compose up
```

If you want to also develop, this command would be helpful
```bash
docker compose up --watch
```

Restart everything
```bash
docker compose down -v && docker compose up --build```